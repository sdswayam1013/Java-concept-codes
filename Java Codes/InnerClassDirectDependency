public class Car{
    // Each Car has its own Engine (non-static inner class)
    private Engine engine = new Engine();

    class Engine{ // Inner class (belongs to Car) and engine has no existence on its own and dependent on car
        void start(){
            System.out.println("Engine is started");
        }
    }
    void move(){
             System.out.println("Car is moving");
             engine.start();
        }

    public static void main(String[] args){
        Car c = new Car();// at the time object craetion takes place for car at the same time Line 2 also gets executed and 
                        // an object is formed for engine too .
        c.move();
    }
}